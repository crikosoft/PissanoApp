@model IEnumerable<PissanoApp.Models.Requerimiento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Obra.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prioridad.nombre)
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Obra)*@
            Obra
        </th>
        <th>
            @Html.DisplayNameFor(model => model.numero)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.comentario)
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Obra.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prioridad.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fechaCreacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.numero)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.comentario)
        </td>

        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.requerimientoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.requerimientoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.requerimientoId })
        </td>
    </tr>
}

</table>
